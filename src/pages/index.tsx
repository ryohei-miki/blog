import type { NextPage } from "next";
import { Box, Flex } from "@chakra-ui/react";

const Home: NextPage = () => {
  return (
    <Flex>
      <Box>Hello</Box>
      <Box>🐱</Box>
    </Flex>
  );
};

export default Home;
